import produce from 'immer';
import { create } from 'zustand';

type States = {
  //
};

type Actions = {
  //
};

const useSnackbarStore = create<States & Actions>((set) => ({}));

export default useSnackbarStore;
